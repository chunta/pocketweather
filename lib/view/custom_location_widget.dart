import 'package:flutter/material.dart';

class CustomLocationWidget extends StatelessWidget {
  const CustomLocationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Custom Location Widget");
  }
}
